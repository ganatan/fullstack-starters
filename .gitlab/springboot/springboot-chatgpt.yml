workflow:
  rules:
    - changes:
        - springboot/springboot-chatgpt/**/*
    - when: never

image: maven:3.9-eclipse-temurin-21

stages:
  - install
  - lint
  - test
  - build

.default-springboot-chatgpt:
  variables:
    PROJECT_DIR: springboot/springboot-chatgpt
  cache:
    key: springboot-chatgpt
    paths:
      - $PROJECT_DIR/.m2/repository
    policy: pull-push
  artifacts:
    paths:
      - $PROJECT_DIR/.m2/repository
    expire_in: 1h

install:springboot-chatgpt:
  stage: install
  extends: .default-springboot-chatgpt
  script:
    - echo "Installing dependencies"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests verify

lint:springboot-chatgpt:
  stage: lint
  extends: .default-springboot-chatgpt
  dependencies:
    - install:springboot-chatgpt
  script:
    - echo "Running chatgpt"
    - cd $PROJECT_DIR
    - mvn -B checkstyle:check

test:springboot-chatgpt:
  stage: test
  extends: .default-springboot-chatgpt
  dependencies:
    - install:springboot-chatgpt
  script:
    - echo "Running unit tests"
    - cd $PROJECT_DIR
    - mvn -B clean test
  artifacts:
    when: always
    reports:
      junit: $PROJECT_DIR/target/surefire-reports/*.xml
    paths:
      - $PROJECT_DIR/target/surefire-reports/

build:springboot-chatgpt:
  stage: build
  extends: .default-springboot-chatgpt
  dependencies:
    - install:springboot-chatgpt
  script:
    - echo "Building Spring Boot JAR"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests package
  artifacts:
    paths:
      - $PROJECT_DIR/target/*.jar
      - $PROJECT_DIR/target/*.war
    expire_in: 1 week
