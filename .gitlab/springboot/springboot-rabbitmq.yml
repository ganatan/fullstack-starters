workflow:
  rules:
    - changes:
        - springboot/springboot-rabbitmq/**/*
    - when: never

image: maven:3.9-eclipse-temurin-21

stages:
  - install
  - test
  - build

.default-springboot-rabbitmq:
  variables:
    PROJECT_DIR: springboot/springboot-rabbitmq
  cache:
    key: springboot-rabbitmq
    paths:
      - $PROJECT_DIR/.m2/repository
    policy: pull-push
  artifacts:
    paths:
      - $PROJECT_DIR/.m2/repository
    expire_in: 1h

install:springboot-rabbitmq:
  stage: install
  extends: .default-springboot-rabbitmq
  script:
    - echo "Installing dependencies"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests verify

test:springboot-rabbitmq:
  stage: test
  extends: .default-springboot-rabbitmq
  dependencies:
    - install:springboot-rabbitmq
  script:
    - echo "Running unit tests"
    - cd $PROJECT_DIR
    - mvn -B clean test
  artifacts:
    when: always
    reports:
      junit: $PROJECT_DIR/target/surefire-reports/*.xml
    paths:
      - $PROJECT_DIR/target/surefire-reports/

build:springboot-rabbitmq:
  stage: build
  extends: .default-springboot-rabbitmq
  dependencies:
    - install:springboot-rabbitmq
  script:
    - echo "Building Spring Boot JAR"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests package
  artifacts:
    paths:
      - $PROJECT_DIR/target/*.jar
      - $PROJECT_DIR/target/*.war
    expire_in: 1 week
