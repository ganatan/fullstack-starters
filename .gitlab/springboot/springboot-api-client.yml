workflow:
  rules:
    - changes:
        - springboot/springboot-api-client/**/*
    - when: never

image: maven:3.9-eclipse-temurin-21

stages:
  - install
  - lint
  - test
  - build

.default-springboot-api-client:
  variables:
    PROJECT_DIR: springboot/springboot-api-client
  cache:
    key: springboot-api-client
    paths:
      - $PROJECT_DIR/.m2/repository
    policy: pull-push
  artifacts:
    paths:
      - $PROJECT_DIR/.m2/repository
    expire_in: 1h

install:springboot-api-client:
  stage: install
  extends: .default-springboot-api-client
  script:
    - echo "Installing dependencies"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests verify

lint:springboot-api-client:
  stage: lint
  extends: .default-springboot-api-client
  dependencies:
    - install:springboot-api-client
  script:
    - echo "Running api-client"
    - cd $PROJECT_DIR
    - mvn -B api-client:check

test:springboot-api-client:
  stage: test
  extends: .default-springboot-api-client
  dependencies:
    - install:springboot-api-client
  script:
    - echo "Running unit tests"
    - cd $PROJECT_DIR
    - mvn -B clean test
  artifacts:
    when: always
    reports:
      junit: $PROJECT_DIR/target/surefire-reports/*.xml
    paths:
      - $PROJECT_DIR/target/surefire-reports/

build:springboot-api-client:
  stage: build
  extends: .default-springboot-api-client
  dependencies:
    - install:springboot-api-client
  script:
    - echo "Building Spring Boot JAR"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests package
  artifacts:
    paths:
      - $PROJECT_DIR/target/*.jar
      - $PROJECT_DIR/target/*.war
    expire_in: 1 week
