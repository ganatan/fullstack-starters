workflow:
  rules:
    - changes:
        - springboot/springboot-kafka/**/*
    - when: never

image: maven:3.9-eclipse-temurin-21

stages:
  - install
  - test
  - build

.default-springboot-kafka:
  variables:
    PROJECT_DIR: springboot/springboot-kafka
  cache:
    key: springboot-kafka
    paths:
      - $PROJECT_DIR/.m2/repository
    policy: pull-push
  artifacts:
    paths:
      - $PROJECT_DIR/.m2/repository
    expire_in: 1h

install:springboot-kafka:
  stage: install
  extends: .default-springboot-kafka
  script:
    - echo "Installing dependencies"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests verify

test:springboot-kafka:
  stage: test
  extends: .default-springboot-kafka
  dependencies:
    - install:springboot-kafka
  script:
    - echo "Running unit tests"
    - cd $PROJECT_DIR
    - mvn -B clean test
  artifacts:
    when: always
    reports:
      junit: $PROJECT_DIR/target/surefire-reports/*.xml
    paths:
      - $PROJECT_DIR/target/surefire-reports/

build:springboot-kafka:
  stage: build
  extends: .default-springboot-kafka
  dependencies:
    - install:springboot-kafka
  script:
    - echo "Building Spring Boot JAR"
    - cd $PROJECT_DIR
    - mvn -B -DskipTests package
  artifacts:
    paths:
      - $PROJECT_DIR/target/*.jar
      - $PROJECT_DIR/target/*.war
    expire_in: 1 week
